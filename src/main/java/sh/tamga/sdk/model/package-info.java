/**
 * Wire-format model types: license/machine/policy resources and the enums that describe their
 * server-side behavior ({@link sh.tamga.sdk.model.ValidationCode} and friends).
 *
 * <p><b>STUB -- scaffolding only.</b> See {@code docs/plans/tamga-java.plan.md} Sections D, H,
 * and L for the full task breakdown this package will eventually satisfy. Several enums in this
 * package model server-declared values that are not actually reachable yet -- read each type's
 * class-level Javadoc before assuming a value will ever appear on the wire.
 */
package sh.tamga.sdk.model;
